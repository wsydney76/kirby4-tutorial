{% extends "@templates/_layouts/main.twig" %}


{% switch currentSite.handle %}

{% case "de" %}
    {% set site = craft.app.sites.siteByHandle('en') %}

    {% set title = 'Seite ohne page' %}
    {% set content = 'Beispiel für eine Seite ohne page' %}


{% default %}
    {% set site = craft.app.sites.siteByHandle('de') %}

    {% set title = 'Page without page' %}
    {% set content = 'Example for a page without page' %}

{% endswitch %}


{% set featuredImage = craft.assets.kind('image').width('>900').orderBy('rand()').one %}

{% block content %}
    {{ content }}
{% endblock %}
